## fbt reference entry point scripts

This folder contains reference scripts that are used as entry points for the fbt build tool on Windows, Linux and macOS.

These scripts must be deployed to project root. They expect the contents of this repository to be available at `scripts/toolchain` path relative to the project root.
